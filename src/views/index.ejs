<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/index.css">
  <title>Kiosk Bot V1 | <%= title %>
  </title>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" crossorigin="anonymous"
    referrerpolicy="no-referrer"></script>
</head>

<body class="max-h-screen relative">
  <main class="flex flex-col items-center">

    <div class="flex">
      <img class="img-fluid" style="max-height: 150px;" src="/images/qrcode_agent-kiosk-bot.onrender.com.png"
        alt="Shop QR Code">
      <div class="flex flex-col justify-center">
        <h1 class="text-red-300">Welcome, your personal assistant awaits you 🤖</h1>
        <p>Setup takes less than 5 minutes to boost your productivity long term</p>
        <small>This is still a work in progress...</small>
      </div>
    </div>
    <div id="content" class="flex items-center justify-center my-14" style="height: 80vh;">
      <div class="bg-gradient rounded-lg shadow-lg p-6 w-full max-w-md" style="height: inherit;">
        <div class="flex space-x-14 p-4">
          <button onclick="window.history.back();">Back</button>
          <h2 class="text-center text-2xl font-bold text-white mx-5">Payment Options</h2>
        </div>
        <%- include('pages/'+page) %>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>

<script>
  $(document).ready(function () {

    $('header a').forEach(link => {
      link.addEventListener('click', (event) => {
        event.preventDefault();
        const url = event.target.getAttribute('href');
        loadContent(url);
      });
    });
  })

  function loadContent(url) {
    fetch(url)
      .then(response => response.text())
      .then(data => {
        document.getElementById('content').innerHTML = data;
      })
      .catch(error => {
        console.error('Error:', error);
      });
  }
</script>

</html>